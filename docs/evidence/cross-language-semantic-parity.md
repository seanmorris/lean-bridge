# Cross-Language Semantic Parity Evidence

Status: JavaScript, Python, C, and Rust packages derive from one semantic contract. JavaScript and Python also pass a fixture that combines finite generic instantiation with asynchronous delivery.

## Run the gate

```sh
npm run binding-parity
```

The command reads the Alpha Binding IR, generates all four packages, and emits a machine-readable report. The report contains one semantic contract hash and one package record per target. Each package record must identify the same component and canonical Binding IR hash.

## Shared contract

`compileBindingSemanticContract` resolves these facts before a backend emits source:

- callable parameters, receivers, results, and result modes;
- finite generic instantiations;
- copied values and identity-bearing resources;
- field and receiver mutability;
- copy, borrow, lease, transfer, lifetime anchor, and disposal rules;
- declared and unexpected error behavior;
- target capabilities and effects;
- documentation and source provenance; and
- proved, trusted-boundary, and unverified assurance records.

The contract keeps rich primitive types as structured type references. A `uint32`, Unicode string, byte sequence, and typed array remain distinct. No contract step replaces the record with serialized text.

## Target projections

The parity report states the host form used for each semantic category:

| Target | Copied values | Identity values | Errors | Cleanup |
|---|---|---|---|---|
| JavaScript | typed objects | classes | exceptions | `dispose` and queued finalization |
| Python | frozen dataclasses | classes and context managers | exceptions | `close`, context management, and finalization |
| C | typed structs | opaque structs | status and typed error output | generated clear and dispose functions |
| Rust | owned structs | structs with borrowed receivers | `Result` | `Drop` |

Result delivery also stays explicit. JavaScript maps a promise result to `Promise`; Python maps the same contract to `Awaitable`. C and Rust currently accept the synchronous value profile. A backend cannot appear in a report if it generated a package without a declared delivery mapping.

## One generic policy

`compileFiniteGenericSpecializations` owns finite generic validation and branch identity. The JavaScript projection and the C, Python, and Rust generators consume that compiler. A branch cannot acquire a different type, identifier, or runtime guard because one target copied an older policy.

## Executed checks

`tests/binding-semantic-parity.test.mjs` checks:

- callable shape, ownership, lifetimes, errors, documentation, and assurance resolution;
- one Binding IR and semantic contract identity across JavaScript, Python, C, and Rust;
- finite `uint32` and string instantiations across JavaScript and Python;
- `Promise` and `Awaitable` delivery from one asynchronous declaration;
- identical copied-value, object-identity, callback, closure, and cleanup-counter results in generated JavaScript and Python packages; and
- complete machine-readable output from the CLI.

The generated package drift gate remains separate. Semantic parity checks meaning. The package gate checks every generated byte.
