# React consumers

The [JavaScript and TypeScript guide](javascript-typescript.md#react) now includes the complete React application, effect ownership, and worker example.

## Run the example

Copy and run the [installed React fixture](javascript-typescript.md#react).

## Load from an effect

Use the [effect example](javascript-typescript.md#load-from-an-effect) to show loading and errors while preventing stale updates after cleanup.

## Preserve binary asset URLs

Use the [React build configuration](javascript-typescript.md#configure-the-react-build) and shared [deployment steps](javascript-typescript.md#build-and-serve-browser-assets).

## Handle load failures

See [installation and loading diagnostics](javascript-typescript.md#diagnose-an-install-or-call-failure). [Worker termination](javascript-typescript.md#terminate-the-worker-with-its-owner) stops computation when its owner leaves.

## Check the author's exact package

Run the [installed-package acceptance command](contributing/testing.md#javascript-and-browser-acceptance) against the author's original archives.
