name: Downstream consumer contract

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: consumer-contract-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"
  LEAN_BRIDGE_CONSUMER_PERFORMANCE_DIR: build/consumer-ci/performance

jobs:
  reviewed-ir-admission:
    name: Reviewed IR admission corpus
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Check reviewed analysis and blocked builds against fresh source oracles
        run: |
          bash scripts/bootstrap-toolchains.sh --lean-only
          source scripts/env.sh
          npm run test:type-corpus:reviewed
          LEAN_BRIDGE_REVIEWED_SOURCE_TEST=1 node --test tests/reviewed-source-build.test.mjs
      - name: Preserve reviewed admission evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-reviewed-ir-${{ github.sha }}
          path: build/type-corpus/reviewed-ir.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30

  nix-cache:
    name: Signed Nix cache recipes
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Install the documented Nix CLI
        uses: cachix/install-nix-action@v31
        with:
          install_url: https://releases.nixos.org/nix/nix-2.24.11/install
      - name: Execute the offline signing and loopback fetch acceptance
        run: npm run test:nix-cache

  documentation:
    name: Documentation contract
    runs-on: ubuntu-24.04
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Check documentation and the versioned support contract
        run: npm run test:docs

  node-consumers:
    name: Node and browser npm consumers
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Install documentation browser engines
        run: npx playwright install --with-deps chromium firefox webkit
      - name: Package and execute clean Node consumers
        id: consumer
        continue-on-error: true
        env:
          LEAN_BRIDGE_DOCUMENTATION_BROWSERS: chromium,firefox,webkit
        run: npm run test:consumer:node
      - name: Prepare the standalone CLI archive with its prebuilt runtime
        run: npm run build:cli-package -- --output build/cli-package --runtime build/consumer-ci-runtime/lazy
      - name: Compile locked and dependency-free projects and install relocated npm releases offline
        env:
          LEAN_BRIDGE_LAKE_WASM_TEST: "1"
          LEAN_BRIDGE_LAKE_GENERATED_PACKAGES_TEST: "1"
          LEAN_BRIDGE_LAKE_ENGINE: build/locked-lake-engine/bin/lean-bridge-component-engine
          LEAN_BRIDGE_LAKE_RUNTIME_ROOT: build/consumer-ci-runtime/lazy
        run: |
          nix build .#component-build-engine --out-link build/locked-lake-engine
          node --test tests/component-publication.test.mjs
          node --test tests/lake-wasm.test.mjs
          node --test tests/unlocked-component.test.mjs
          node --test --test-name-pattern='npm releases|publication dry run' tests/lake-generated-packages.test.mjs
      - name: Compare installed Node, browser, React and worker corpus packages with fresh Lean
        id: type_corpus_npm
        continue-on-error: true
        env:
          LEAN_BRIDGE_LAKE_ENGINE: build/locked-lake-engine/bin/lean-bridge-component-engine
          LEAN_BRIDGE_LAKE_RUNTIME_ROOT: build/consumer-ci-runtime/lazy
          LEAN_BRIDGE_TYPE_CORPUS_BROWSERS: chromium,firefox,webkit
        run: |
          bash scripts/bootstrap-toolchains.sh --lean-only
          npm run test:type-corpus:npm
          npm run test:type-corpus:reviewed-npm
          node --test tests/component-char.test.mjs
          node --test tests/component-words.test.mjs
          node --test tests/component-callables.test.mjs
          node --test tests/component-arrays.test.mjs
          node --test tests/component-records.test.mjs
          node --test tests/component-compounds.test.mjs
          node --test tests/component-lists.test.mjs
          node --test tests/component-variant-runtime.test.mjs tests/component-variants.test.mjs
          node --test tests/component-aliases.test.mjs
          test -s build/callables/npm/report.json
          test -s build/arrays/npm/report.json
          test -s build/records/npm/report.json
          test -s build/compounds/npm/report.json
          test -s build/compounds/recordless/report.json
          test -s build/lists/npm/report.json
          test -s build/variants/npm/report.json
          test -s build/aliases/npm/report.json
          test -s build/word-npm/report.json
          test -s build/char-npm/report.json
          test -s build/type-corpus/browser-javascript-browser-react-browser-worker-node-javascript-node-typescript.json
          test -s build/type-corpus/reviewed-wasm-browser-javascript-browser-react-browser-worker-node-javascript-node-typescript.json
      - name: Preserve installed npm corpus observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-npm-${{ github.sha }}
          path: |
            build/type-corpus/browser-javascript-browser-react-browser-worker-node-javascript-node-typescript.json
            build/type-corpus/reviewed-wasm-browser-javascript-browser-react-browser-worker-node-javascript-node-typescript.json
            build/char-npm/
            build/word-npm/
            build/callables/npm/
            build/arrays/npm/
            build/records/npm/
            build/compounds/
            build/lists/npm/
            build/variants/npm/
            build/aliases/npm/
          if-no-files-found: error
          retention-days: 30
      - name: Install C# for the combined native projection
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '8.0.424'
      - name: Install Java for the combined native projection
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '22.0.2'
      - name: Install Ruby for the combined native projection
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
      - name: Build one captured API for npm, CPAN, C, C++, NuGet, Maven, RubyGems, WIT, PyPI, Cargo and PHP and execute installations
        env:
          LEAN_BRIDGE_MULTI_PROFILE_TEST: "1"
          LEAN_BRIDGE_LAKE_ENGINE: build/locked-lake-engine/bin/lean-bridge-component-engine
          LEAN_BRIDGE_LAKE_RUNTIME_ROOT: build/consumer-ci-runtime/lazy
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential libperl-dev pkg-config python3-venv php-cli php-common composer zstd m4
          bash scripts/bootstrap-toolchains.sh --lean-only
          bash scripts/bootstrap-toolchains.sh --wasm-tools-only
          bash scripts/bootstrap-rust-ci.sh
          export LEAN_BRIDGE_RUSTC="$PWD/.toolchains/rust-1.90.0/bin/rustc"
          export LEAN_BRIDGE_CARGO="$PWD/.toolchains/rust-1.90.0/bin/cargo"
          nix build .#wasmtime-c-api --out-link build/wasmtime-c-api
          export LEAN_BRIDGE_WASMTIME_C_API="$PWD/build/wasmtime-c-api"
          source scripts/env.sh
          node --test tests/multi-profile-project.test.mjs
      - name: Check documentation API captures through compiler-backed public analysis
        run: LEAN_BRIDGE_BUILD_BACKEND=nix node scripts/capture-reference-apis.mjs
      - name: Build and consume a Lean project through the tarball-installed CLI
        run: npm run acceptance:cli-package -- --candidate build/cli-package --output build/cli-package-acceptance --backend nix
      - name: Install the disposable npm registry
        run: npm install --prefix build/registry-tools --ignore-scripts --no-audit --no-fund verdaccio@6.2.0
      - name: Publish through the installed CLI and consume only the component
        run: >-
          node scripts/check-local-npm-release.mjs
          --verdaccio build/registry-tools/node_modules/.bin/verdaccio
          --candidate build/cli-package --output build/cli-registry-acceptance
          --backend nix --browsers chromium,firefox,webkit
      - name: Preserve the CLI candidate and installed-author evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: cli-package-acceptance-${{ github.sha }}
          path: |
            build/cli-package/*.tgz
            build/cli-package/cli-package-report.json
            build/cli-package-acceptance
            build/cli-registry-acceptance
          if-no-files-found: warn
          retention-days: 30
      - name: Record Node observations
        if: always()
        shell: bash
        run: |
          test_result="${{ steps.consumer.outcome == 'success' && steps.type_corpus_npm.outcome == 'success' && 'passed' || 'failed' }}"
          executed="${{ steps.consumer.outcome == 'success' && steps.type_corpus_npm.outcome == 'success' && 'true' || 'false' }}"
          node scripts/consumer-ci.mjs record --consumer node-javascript --output build/consumer-ci/results/node-javascript.json --performance build/consumer-ci/performance/node-javascript.json --test-result "$test_result" --package-installation "$executed" --real-lean-execution "$executed" --command "npm run test:consumer:node && npm run test:type-corpus:npm && npm run test:type-corpus:reviewed-npm && node --test tests/component-char.test.mjs && node --test tests/component-words.test.mjs && node --test tests/component-callables.test.mjs && node --test tests/component-arrays.test.mjs && node --test tests/component-records.test.mjs && node --test tests/component-compounds.test.mjs && node --test tests/component-lists.test.mjs && node --test tests/component-variant-runtime.test.mjs tests/component-variants.test.mjs && node --test tests/component-aliases.test.mjs"
          node scripts/consumer-ci.mjs record --consumer node-typescript --output build/consumer-ci/results/node-typescript.json --performance build/consumer-ci/performance/node-typescript.json --test-result "$test_result" --package-installation "$executed" --real-lean-execution "$executed" --command "npm run test:consumer:node && npm run test:type-corpus:npm && npm run test:type-corpus:reviewed-npm && node --test tests/component-char.test.mjs && node --test tests/component-words.test.mjs && node --test tests/component-callables.test.mjs && node --test tests/component-arrays.test.mjs && node --test tests/component-records.test.mjs && node --test tests/component-compounds.test.mjs && node --test tests/component-lists.test.mjs && node --test tests/component-variant-runtime.test.mjs tests/component-variants.test.mjs && node --test tests/component-aliases.test.mjs"
      - name: Upload Node observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-results-node-${{ github.sha }}
          path: build/consumer-ci/results/*.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Enforce Node support and the browser corpus
        if: steps.consumer.outcome != 'success' || steps.type_corpus_npm.outcome != 'success'
        run: exit 1
      - name: Preserve executable documentation observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: documentation-consumer-results-${{ github.sha }}
          path: build/documentation-consumer-acceptance
          if-no-files-found: warn
          retention-days: 30

  browser-consumer:
    name: Browser npm boundary
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Install dependencies and browser
        run: |
          npm ci --ignore-scripts
          npx playwright install --with-deps chromium
      - name: Install the npm archive and execute real Lean in Chromium
        id: consumer
        continue-on-error: true
        run: npm run test:consumer:browser
      - name: Record browser observations
        if: always()
        shell: bash
        run: |
          test_result="${{ steps.consumer.outcome == 'success' && 'passed' || 'failed' }}"
          executed="${{ steps.consumer.outcome == 'success' && 'true' || 'false' }}"
          node scripts/consumer-ci.mjs record --consumer browser-javascript --output build/consumer-ci/results/browser-javascript.json --performance build/consumer-ci/performance/browser-javascript.json --test-result "$test_result" --package-installation "$executed" --real-lean-execution "$executed" --command "npm run test:consumer:browser"
      - name: Upload browser observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-results-browser-${{ github.sha }}
          path: build/consumer-ci/results/*.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Enforce the browser boundary
        if: steps.consumer.outcome != 'success'
        run: exit 1

  php-consumers:
    name: Native PHP and PHP-Wasm
    runs-on: ubuntu-24.04
    timeout-minutes: 240
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Restore pinned PHP and compiler inputs
        uses: actions/cache@v5
        with:
          path: |
            .toolchains
            build/php-wasm-sdk
            build/php-wasm-host
          key: consumer-php-${{ runner.os }}-${{ hashFiles('flake.lock', 'lean-toolchain', 'scripts/bootstrap-toolchains.sh', 'scripts/bootstrap-php-wasm-ci.sh', 'poc/lean-link-spike/bindings/php-wasm.package.json') }}
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Install native PHP and PHP-Wasm host build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential autoconf automake bison flex gperf libtool re2c cmake pkg-config php-cli php-common php-zip php-mbstring composer zstd
          bash scripts/bootstrap-php-wasm-ci.sh --check-prerequisites
      - name: Build ordinary PHP packages and execute relocated Composer installations
        id: ordinary_php
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_PHP_TEST: "1"
        run: |
          bash scripts/bootstrap-toolchains.sh --lean-only
          source scripts/env.sh
          node --test tests/php-brick-math.test.mjs tests/native-php.test.mjs
      - name: Compare weak and strict installed Composer callers with fresh Lean
        id: type_corpus_php_native
        continue-on-error: true
        run: |
          export LEAN_BRIDGE_PHP="$(command -v php)"
          export LEAN_BRIDGE_COMPOSER="$(command -v composer)"
          source scripts/env.sh
          npm run test:type-corpus:php-native && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=php-native node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=php-native node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=php-native node --test tests/native-words.test.mjs
          LEAN_BRIDGE_PHP_CALLABLE_TEST=1 node --test tests/php-callables.test.mjs tests/php-callable-contract.test.mjs
          LEAN_BRIDGE_PHP_COMPOUND_TEST=1 node --test tests/php-compounds.test.mjs tests/php-compound-contract.test.mjs
          LEAN_BRIDGE_PHP_LIST_TEST=1 node --test tests/php-lists.test.mjs tests/php-list-contract.test.mjs
          LEAN_BRIDGE_PHP_ALIAS_TEST=1 node --test tests/php-aliases.test.mjs tests/php-alias-contract.test.mjs
          test -s build/aliases/php-native.json
          test -s build/lists/php-native.json
          test -s build/type-corpus/php-native.json
          test -s build/type-corpus/reviewed-native-php-native.json
      - name: Upload installed PHP corpus observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-php-native-${{ github.sha }}
          path: |
            build/type-corpus/php-native.json
            build/type-corpus/reviewed-native-php-native.json
            build/char-native/php-native.json
            build/word-native/php-native.json
            build/callables/php-native.json
            build/compounds/php-native.json
            build/lists/php-native.json
            build/aliases/php-native.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Build twice, install, and execute every PHP transport
        id: consumer
        continue-on-error: true
        run: npm run test:consumer:php
      - name: Exercise generic copied Zend conversions in 32-bit PHP-Wasm
        id: copied_zend
        continue-on-error: true
        env:
          LEAN_BRIDGE_PHP_WASM_ZEND_TEST: "1"
        run: |
          bash scripts/bootstrap-php-wasm-ci.sh
          node --test tests/php-copied-zend.test.mjs
      - name: Compile ordinary Lean APIs and execute installed PHP-Wasm packages
        id: ordinary_php_wasm
        continue-on-error: true
        env:
          LEAN_BRIDGE_PHP_WASM_ORDINARY_TEST: "1"
          LEAN_BRIDGE_PHP_WASM_BROWSER_TEST: "1"
          LEAN_WASM_RUNTIME_PROFILE: browser
          LEAN_WASM_RUNTIME_VARIANT: php-wasm-3.1.68
          LEAN_WASM_ARTIFACT_TARGET: php-wasm-emscripten-3.1.68
        run: |
          npx playwright install --with-deps chromium
          bash scripts/bootstrap-toolchains.sh
          export LEAN_WASM_EMSDK="$GITHUB_WORKSPACE/.toolchains/emsdk-php-wasm"
          bash scripts/build-lean-runtime.sh
          node --test tests/php-wasm-ordinary.test.mjs
      - name: Compare installed PHP-Wasm packages with the shared Lean corpus
        id: type_corpus_php_wasm
        continue-on-error: true
        run: |
          source scripts/env.sh
          npm run test:type-corpus:php-wasm
          npm run test:type-corpus:reviewed-php-wasm
          LEAN_BRIDGE_CHAR_PROFILES=php-wasm node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=php-wasm node --test tests/native-words.test.mjs
          LEAN_BRIDGE_PHP_WASM_CALLABLE_TEST=1 node --test tests/php-wasm-callables.test.mjs tests/php-wasm-callable-contract.test.mjs
          LEAN_BRIDGE_PHP_WASM_COMPOUND_TEST=1 node --test tests/php-wasm-compounds.test.mjs tests/php-wasm-compound-contract.test.mjs tests/php-wasm-compound-zend.test.mjs
          test -s build/compounds/php-wasm.json
          test -s build/compounds/php-wasm-zend-faults.json
          LEAN_BRIDGE_PHP_WASM_LIST_TEST=1 node --test tests/php-wasm-lists.test.mjs tests/php-wasm-list-contract.test.mjs tests/php-wasm-list-zend.test.mjs
          test -s build/lists/php-wasm.json
          test -s build/lists/php-wasm-zend-faults.json
          LEAN_BRIDGE_PHP_WASM_ALIAS_TEST=1 node --test --test-concurrency=1 tests/php-wasm-aliases.test.mjs tests/php-wasm-alias-contract.test.mjs tests/php-wasm-alias-zend.test.mjs
          test -s build/aliases/php-wasm.json
          test -s build/aliases/php-wasm-zend-faults.json
          test -s build/type-corpus/php-wasm.json
          test -s build/type-corpus/reviewed-wasm-php-wasm.json
      - name: Upload PHP-Wasm corpus evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-php-wasm-${{ github.sha }}
          path: |
            build/type-corpus/php-wasm.json
            build/type-corpus/reviewed-wasm-php-wasm.json
            build/char-native/php-wasm.json
            build/word-native/php-wasm.json
            build/callables/php-wasm.json
            build/compounds/php-wasm.json
            build/compounds/php-wasm-zend-faults.json
            build/lists/php-wasm.json
            build/lists/php-wasm-zend-faults.json
            build/aliases/php-wasm.json
            build/aliases/php-wasm-zend-faults.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Combine PHP-Wasm with native PHP and JavaScript from one captured API
        id: php_multi_profile
        continue-on-error: true
        env:
          LEAN_BRIDGE_PHP_MULTI_PROFILE_TEST: "1"
          LEAN_BRIDGE_LAKE_ENGINE: build/php-multi-engine/bin/lean-bridge-component-engine
          LEAN_BRIDGE_LAKE_RUNTIME_ROOT: build/php-multi-runtime/lazy
        run: |
          nix build .#universal-core-artifacts --out-link build/php-multi-runtime
          nix build .#component-build-engine --out-link build/php-multi-engine
          source scripts/env.sh
          node --test tests/php-wasm-multi-profile.test.mjs
          LEAN_BRIDGE_REVIEWED_MULTI_PROFILE_TEST=1 node --test tests/php-wasm-multi-profile.test.mjs
      - name: Record PHP observations
        if: always()
        shell: bash
        run: |
          test_result="${{ steps.consumer.outcome == 'success' && 'passed' || 'failed' }}"
          executed="${{ steps.consumer.outcome == 'success' && 'true' || 'false' }}"
          native_result="$test_result"
          native_executed="$executed"
          if [ "${{ steps.ordinary_php.outcome }}" != success ] || [ "${{ steps.type_corpus_php_native.outcome }}" != success ]; then
            native_result=failed
            native_executed=false
          fi
          wasm_result="$test_result"
          wasm_executed="$executed"
          if [ "${{ steps.ordinary_php_wasm.outcome }}" != success ] || [ "${{ steps.copied_zend.outcome }}" != success ] || [ "${{ steps.php_multi_profile.outcome }}" != success ] || [ "${{ steps.type_corpus_php_wasm.outcome }}" != success ]; then
            wasm_result=failed
            wasm_executed=false
          fi
          node scripts/consumer-ci.mjs record --consumer php-native --output build/consumer-ci/results/php-native.json --performance build/consumer-ci/performance/php-native.json --test-result "$native_result" --package-installation "$native_executed" --real-lean-execution "$native_executed" --command "npm run test:consumer:php && LEAN_BRIDGE_NATIVE_PHP_TEST=1 node --test tests/native-php.test.mjs && npm run test:type-corpus:php-native && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=php-native node --test tests/type-corpus-reviewed-native.test.mjs && LEAN_BRIDGE_PHP_CALLABLE_TEST=1 node --test tests/php-callables.test.mjs tests/php-callable-contract.test.mjs && LEAN_BRIDGE_PHP_COMPOUND_TEST=1 node --test tests/php-compounds.test.mjs tests/php-compound-contract.test.mjs && LEAN_BRIDGE_PHP_LIST_TEST=1 node --test tests/php-lists.test.mjs tests/php-list-contract.test.mjs && LEAN_BRIDGE_PHP_ALIAS_TEST=1 node --test tests/php-aliases.test.mjs tests/php-alias-contract.test.mjs"
          node scripts/consumer-ci.mjs record --consumer php-wasm --output build/consumer-ci/results/php-wasm.json --performance build/consumer-ci/performance/php-wasm.json --test-result "$wasm_result" --package-installation "$wasm_executed" --real-lean-execution "$wasm_executed" --command "npm run test:consumer:php && LEAN_BRIDGE_PHP_WASM_ZEND_TEST=1 node --test tests/php-copied-zend.test.mjs && LEAN_BRIDGE_PHP_WASM_ORDINARY_TEST=1 LEAN_BRIDGE_PHP_WASM_BROWSER_TEST=1 node --test tests/php-wasm-ordinary.test.mjs && LEAN_BRIDGE_PHP_MULTI_PROFILE_TEST=1 node --test tests/php-wasm-multi-profile.test.mjs && LEAN_BRIDGE_REVIEWED_MULTI_PROFILE_TEST=1 node --test tests/php-wasm-multi-profile.test.mjs && npm run test:type-corpus:php-wasm && npm run test:type-corpus:reviewed-php-wasm && LEAN_BRIDGE_PHP_WASM_CALLABLE_TEST=1 node --test tests/php-wasm-callables.test.mjs tests/php-wasm-callable-contract.test.mjs && LEAN_BRIDGE_PHP_WASM_COMPOUND_TEST=1 node --test tests/php-wasm-compounds.test.mjs tests/php-wasm-compound-contract.test.mjs tests/php-wasm-compound-zend.test.mjs && LEAN_BRIDGE_PHP_WASM_LIST_TEST=1 node --test tests/php-wasm-lists.test.mjs tests/php-wasm-list-contract.test.mjs tests/php-wasm-list-zend.test.mjs && LEAN_BRIDGE_PHP_WASM_ALIAS_TEST=1 node --test --test-concurrency=1 tests/php-wasm-aliases.test.mjs tests/php-wasm-alias-contract.test.mjs tests/php-wasm-alias-zend.test.mjs"
      - name: Upload PHP observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-results-php-${{ github.sha }}
          path: build/consumer-ci/results/*.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Enforce PHP support
        if: steps.consumer.outcome != 'success' || steps.ordinary_php.outcome != 'success' || steps.type_corpus_php_native.outcome != 'success' || steps.copied_zend.outcome != 'success' || steps.ordinary_php_wasm.outcome != 'success' || steps.php_multi_profile.outcome != 'success' || steps.type_corpus_php_wasm.outcome != 'success'
        run: exit 1

  native-consumers:
    name: Python, Rust, C, and C++
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Install every package and execute real Lean
        id: consumer
        continue-on-error: true
        run: npm run test:consumer:native
      - name: Compile ordinary Lean APIs once for relocated C and C++ consumers
        id: ordinary_c
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_C_TEST: "1"
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential cmake pkg-config zstd m4
          bash scripts/bootstrap-toolchains.sh --lean-only
          source scripts/env.sh
          node --test tests/native-c-family.test.mjs tests/native-c-copied.test.mjs
      - name: Compare relocated C and C++ corpus consumers with fresh Lean
        id: type_corpus_c_family
        continue-on-error: true
        run: |
          source scripts/env.sh
          npm run test:type-corpus:c-family && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=c,cpp node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=c,cpp node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=c,cpp node --test tests/native-words.test.mjs
          LEAN_BRIDGE_NATIVE_COMPOUND_TEST=1 node --test tests/native-compounds.test.mjs
          test -s build/compounds/native.json
          LEAN_BRIDGE_NATIVE_LIST_TEST=1 node --test tests/native-lists.test.mjs
          test -s build/lists/native.json
          LEAN_BRIDGE_NATIVE_ALIAS_TEST=1 node --test tests/native-aliases.test.mjs
          test -s build/aliases/native.json
          LEAN_BRIDGE_CPP_VARIANT_TEST=1 node --test tests/cpp-variants.test.mjs
          test -s build/variants/cpp.json
          LEAN_BRIDGE_C_CALLABLE_TEST=1 node --test tests/c-callables.test.mjs
          test -s build/callables/c.json
          LEAN_BRIDGE_CPP_CALLABLE_TEST=1 node --test tests/cpp-callables.test.mjs
          test -s build/callables/cpp.json
          test -s build/type-corpus/c-cpp.json
          test -s build/type-corpus/reviewed-native-c-cpp.json
      - name: Upload installed C and C++ corpus observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-c-family-${{ github.sha }}
          path: |
            build/type-corpus/c-cpp.json
            build/type-corpus/reviewed-native-c-cpp.json
            build/char-native/c-cpp.json
            build/word-native/c-cpp.json
            build/callables/c.json
            build/callables/cpp.json
            build/compounds/native.json
            build/lists/native.json
            build/aliases/native.json
            build/variants/cpp.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Build ordinary Lean APIs and install relocated Python wheels
        id: ordinary_python
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_PYTHON_TEST: "1"
        run: |
          sudo apt-get update && sudo apt-get install -y python3-venv
          source scripts/env.sh
          node --test tests/native-python.test.mjs
      - name: Compare installed Python corpus packages with fresh Lean results
        id: type_corpus_python
        continue-on-error: true
        run: |
          source scripts/env.sh
          npm run test:type-corpus:python && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=python node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=python node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=python node --test tests/native-words.test.mjs
          LEAN_BRIDGE_PYTHON_CALLABLE_TEST=1 node --test tests/python-callables.test.mjs
          test -s build/callables/python.json
          LEAN_BRIDGE_PYTHON_COMPOUND_TEST=1 node --test tests/python-compounds.test.mjs
          test -s build/compounds/python.json
          LEAN_BRIDGE_PYTHON_LIST_TEST=1 node --test tests/python-lists.test.mjs
          test -s build/lists/python.json
          python3 -m venv build/python-alias-typecheck
          build/python-alias-typecheck/bin/python -m pip install --no-cache-dir mypy==1.17.1 typing_extensions==4.16.0 mypy_extensions==1.1.0 pathspec==1.1.1
          LEAN_BRIDGE_PYTHON_ALIAS_TEST=1 node --test tests/python-aliases.test.mjs
          test -s build/aliases/python.json
          test -s build/type-corpus/python.json
          test -s build/type-corpus/reviewed-native-python.json
      - name: Upload the real-Lean type corpus report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-python-${{ github.sha }}
          path: |
            build/type-corpus/python.json
            build/type-corpus/reviewed-native-python.json
            build/char-native/python.json
            build/word-native/python.json
            build/callables/python.json
            build/compounds/python.json
            build/lists/python.json
            build/aliases/python.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Build ordinary Lean APIs and execute relocated Cargo crates
        id: ordinary_rust
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_RUST_TEST: "1"
        run: |
          bash scripts/bootstrap-rust-ci.sh
          export LEAN_BRIDGE_RUSTC="$PWD/.toolchains/rust-1.90.0/bin/rustc"
          export LEAN_BRIDGE_CARGO="$PWD/.toolchains/rust-1.90.0/bin/cargo"
          source scripts/env.sh
          node --test tests/native-rust.test.mjs
      - name: Compare isolated Cargo corpus consumers with fresh Lean
        id: type_corpus_rust
        continue-on-error: true
        run: |
          bash scripts/bootstrap-rust-ci.sh
          export LEAN_BRIDGE_RUSTC="$PWD/.toolchains/rust-1.90.0/bin/rustc"
          export LEAN_BRIDGE_CARGO="$PWD/.toolchains/rust-1.90.0/bin/cargo"
          source scripts/env.sh
          npm run test:type-corpus:rust && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=rust node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=rust node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=rust node --test tests/native-words.test.mjs
          LEAN_BRIDGE_RUST_CALLABLE_TEST=1 node --test tests/rust-callables.test.mjs
          LEAN_BRIDGE_RUST_COMPOUND_TEST=1 node --test tests/rust-compounds.test.mjs
          test -s build/compounds/rust.json
          LEAN_BRIDGE_RUST_LIST_TEST=1 node --test tests/rust-lists.test.mjs
          test -s build/lists/rust.json
          LEAN_BRIDGE_RUST_ALIAS_TEST=1 node --test tests/rust-aliases.test.mjs
          test -s build/aliases/rust.json
          test -s build/type-corpus/rust.json
          test -s build/type-corpus/reviewed-native-rust.json
      - name: Upload installed Rust corpus observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-rust-${{ github.sha }}
          path: |
            build/type-corpus/rust.json
            build/type-corpus/reviewed-native-rust.json
            build/char-native/rust.json
            build/word-native/rust.json
            build/callables/rust.json
            build/compounds/rust.json
            build/lists/rust.json
            build/aliases/rust.json
          if-no-files-found: error
          retention-days: 30
      - name: Record native consumer observations
        if: always()
        shell: bash
        run: |
          test_result="${{ steps.consumer.outcome == 'success' && 'passed' || 'failed' }}"
          executed="${{ steps.consumer.outcome == 'success' && 'true' || 'false' }}"
          for consumer in python rust c cpp; do
            result="$test_result"
            installed="$executed"
            consumer_command="npm run test:consumer:native"
            if [ "$consumer" = c ] || [ "$consumer" = cpp ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_NATIVE_C_TEST=1 node --test tests/native-c-family.test.mjs tests/native-c-copied.test.mjs && npm run test:type-corpus:c-family && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=c,cpp node --test tests/type-corpus-reviewed-native.test.mjs"
              if [ "${{ steps.ordinary_c.outcome }}" != success ] || [ "${{ steps.type_corpus_c_family.outcome }}" != success ]; then
                result=failed
                installed=false
              fi
            fi
            if [ "$consumer" = c ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_C_CALLABLE_TEST=1 node --test tests/c-callables.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_CPP_CALLABLE_TEST=1 node --test tests/cpp-callables.test.mjs"
            fi
            if [ "$consumer" = python ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_NATIVE_PYTHON_TEST=1 node --test tests/native-python.test.mjs && npm run test:type-corpus:python && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=python node --test tests/type-corpus-reviewed-native.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_PYTHON_CALLABLE_TEST=1 node --test tests/python-callables.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_PYTHON_COMPOUND_TEST=1 node --test tests/python-compounds.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_PYTHON_LIST_TEST=1 node --test tests/python-lists.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_PYTHON_ALIAS_TEST=1 node --test tests/python-aliases.test.mjs"
              if [ "${{ steps.ordinary_python.outcome }}" != success ] || [ "${{ steps.type_corpus_python.outcome }}" != success ]; then
                result=failed
                installed=false
              fi
            fi
            if [ "$consumer" = rust ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_NATIVE_RUST_TEST=1 node --test tests/native-rust.test.mjs && npm run test:type-corpus:rust && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=rust node --test tests/type-corpus-reviewed-native.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUST_CALLABLE_TEST=1 node --test tests/rust-callables.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUST_COMPOUND_TEST=1 node --test tests/rust-compounds.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUST_LIST_TEST=1 node --test tests/rust-lists.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUST_ALIAS_TEST=1 node --test tests/rust-aliases.test.mjs"
              if [ "${{ steps.ordinary_rust.outcome }}" != success ] || [ "${{ steps.type_corpus_rust.outcome }}" != success ]; then
                result=failed
                installed=false
              fi
            fi
            node scripts/consumer-ci.mjs record --consumer "$consumer" --output "build/consumer-ci/results/$consumer.json" --performance "build/consumer-ci/performance/$consumer.json" --test-result "$result" --package-installation "$installed" --real-lean-execution "$installed" --command "$consumer_command"
          done
      - name: Upload native consumer observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-results-native-${{ github.sha }}
          path: build/consumer-ci/results/*.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Enforce native consumer support
        if: steps.consumer.outcome != 'success' || steps.ordinary_c.outcome != 'success' || steps.type_corpus_c_family.outcome != 'success' || steps.ordinary_python.outcome != 'success' || steps.type_corpus_python.outcome != 'success' || steps.ordinary_rust.outcome != 'success' || steps.type_corpus_rust.outcome != 'success'
        run: exit 1

  managed-consumers:
    name: .NET, JVM, and Ruby
    runs-on: ubuntu-24.04
    timeout-minutes: 240
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Restore packages and execute real Lean
        id: consumer
        continue-on-error: true
        run: npm run test:consumer:managed
      - name: Install the ordinary C# compiler
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '8.0.424'
      - name: Compile ordinary Lean APIs and install relocated NuGet releases
        id: ordinary_dotnet
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_DOTNET_TEST: "1"
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential zstd m4
          bash scripts/bootstrap-toolchains.sh --lean-only
          source scripts/env.sh
          node --test tests/native-dotnet.test.mjs
      - name: Compare installed NuGet corpus packages with fresh Lean results
        id: type_corpus_dotnet
        continue-on-error: true
        run: |
          export LEAN_BRIDGE_DOTNET="$(command -v dotnet)"
          source scripts/env.sh
          npm run test:type-corpus:dotnet && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=dotnet node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=dotnet node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=dotnet node --test tests/native-words.test.mjs
          LEAN_BRIDGE_DOTNET_CALLABLE_TEST=1 node --test tests/dotnet-callables.test.mjs tests/dotnet-callable-contract.test.mjs
          LEAN_BRIDGE_DOTNET_COMPOUND_TEST=1 node --test tests/dotnet-compounds.test.mjs tests/dotnet-compound-contract.test.mjs
          test -s build/compounds/dotnet.json
          LEAN_BRIDGE_DOTNET_LIST_TEST=1 node --test tests/dotnet-lists.test.mjs tests/dotnet-list-contract.test.mjs
          test -s build/lists/dotnet.json
          LEAN_BRIDGE_DOTNET_ALIAS_TEST=1 node --test tests/dotnet-aliases.test.mjs
          test -s build/aliases/dotnet.json
          test -s build/type-corpus/dotnet.json
          test -s build/type-corpus/reviewed-native-dotnet.json
      - name: Upload the .NET real-Lean type corpus report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-dotnet-${{ github.sha }}
          path: |
            build/type-corpus/dotnet.json
            build/type-corpus/reviewed-native-dotnet.json
            build/char-native/dotnet.json
            build/word-native/dotnet.json
            build/callables/dotnet.json
            build/compounds/dotnet.json
            build/lists/dotnet.json
            build/aliases/dotnet.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Install the ordinary Java compiler
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '22.0.2'
      - name: Compile ordinary Lean APIs and install Maven packages in Java and Kotlin
        id: ordinary_jvm
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_JVM_TEST: "1"
        run: |
          sudo apt-get install -y maven unzip
          curl -fL --retry 3 --output build/kotlin-compiler.zip https://github.com/JetBrains/kotlin/releases/download/v2.2.0/kotlin-compiler-2.2.0.zip
          echo '1adb6f1a5845ba0aa5a59e412e44c8e405236b957de1a9683619f1dca3b16932  build/kotlin-compiler.zip' | sha256sum --check
          unzip -q build/kotlin-compiler.zip -d build/kotlin-tools
          export LEAN_BRIDGE_JAVAC="$JAVA_HOME/bin/javac"
          export LEAN_BRIDGE_JAVA="$JAVA_HOME/bin/java"
          export LEAN_BRIDGE_KOTLINC="$PWD/build/kotlin-tools/kotlinc/bin/kotlinc"
          export LEAN_BRIDGE_KOTLIN="$PWD/build/kotlin-tools/kotlinc/bin/kotlin"
          source scripts/env.sh
          node --test tests/native-jvm.test.mjs
      - name: Compare isolated Java and Kotlin corpus consumers with fresh Lean
        id: type_corpus_jvm
        continue-on-error: true
        run: |
          export LEAN_BRIDGE_JAVAC="$JAVA_HOME/bin/javac"
          export LEAN_BRIDGE_JAVA="$JAVA_HOME/bin/java"
          export LEAN_BRIDGE_MAVEN="$(command -v mvn)"
          export LEAN_BRIDGE_KOTLINC="$PWD/build/kotlin-tools/kotlinc/bin/kotlinc"
          source scripts/env.sh
          npm run test:type-corpus:jvm && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=java,kotlin node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=java,kotlin node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=java,kotlin node --test tests/native-words.test.mjs
          LEAN_BRIDGE_JVM_CALLABLE_TEST=1 node --test tests/jvm-callables.test.mjs tests/jvm-callable-contract.test.mjs
          LEAN_BRIDGE_JVM_COMPOUND_TEST=1 node --test tests/jvm-compounds.test.mjs tests/jvm-compound-contract.test.mjs
          test -s build/compounds/jvm.json
          LEAN_BRIDGE_JVM_LIST_TEST=1 node --test tests/jvm-lists.test.mjs tests/jvm-list-contract.test.mjs
          test -s build/lists/jvm.json
          LEAN_BRIDGE_JVM_ALIAS_TEST=1 node --test tests/jvm-aliases.test.mjs
          test -s build/aliases/jvm.json
          test -s build/type-corpus/java-kotlin.json
          test -s build/type-corpus/reviewed-native-java-kotlin.json
      - name: Upload installed Java and Kotlin corpus observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-jvm-${{ github.sha }}
          path: |
            build/type-corpus/java-kotlin.json
            build/type-corpus/reviewed-native-java-kotlin.json
            build/char-native/java-kotlin.json
            build/word-native/java-kotlin.json
            build/callables/jvm.json
            build/compounds/jvm.json
            build/lists/jvm.json
            build/aliases/jvm.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Install the ordinary Ruby runtime
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
      - name: Build ordinary Lean APIs and install relocated RubyGems releases
        id: ordinary_ruby
        env:
          LEAN_BRIDGE_NATIVE_RUBY_TEST: "1"
        run: |
          export LEAN_BRIDGE_RUBY="$(command -v ruby)"
          export LEAN_BRIDGE_GEM="$(command -v gem)"
          source scripts/env.sh
          node --test tests/native-ruby.test.mjs
      - name: Compare installed Ruby corpus packages with fresh Lean results
        id: type_corpus_ruby
        continue-on-error: true
        run: |
          export LEAN_BRIDGE_RUBY="$(command -v ruby)"
          export LEAN_BRIDGE_GEM="$(command -v gem)"
          source scripts/env.sh
          npm run test:type-corpus:ruby && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=ruby node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=ruby node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=ruby node --test tests/native-words.test.mjs
          LEAN_BRIDGE_RUBY_CALLABLE_TEST=1 node --test tests/ruby-callables.test.mjs
          LEAN_BRIDGE_RUBY_COMPOUND_TEST=1 node --test tests/ruby-compounds.test.mjs tests/ruby-compound-contract.test.mjs
          LEAN_BRIDGE_RUBY_LIST_TEST=1 node --test tests/ruby-lists.test.mjs tests/ruby-list-contract.test.mjs
          LEAN_BRIDGE_RUBY_ALIAS_TEST=1 node --test tests/ruby-aliases.test.mjs
          test -s build/callables/ruby.json
          test -s build/compounds/ruby.json
          test -s build/lists/ruby.json
          test -s build/aliases/ruby.json
          test -s build/type-corpus/ruby.json
          test -s build/type-corpus/reviewed-native-ruby.json
      - name: Upload the Ruby real-Lean type corpus report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-ruby-${{ github.sha }}
          path: |
            build/type-corpus/ruby.json
            build/type-corpus/reviewed-native-ruby.json
            build/char-native/ruby.json
            build/word-native/ruby.json
            build/callables/ruby.json
            build/compounds/ruby.json
            build/lists/ruby.json
            build/aliases/ruby.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Record managed consumer observations
        if: always()
        shell: bash
        run: |
          for consumer in dotnet jvm ruby; do
            consumer_command="npm run test:consumer:managed"
            test_result="${{ steps.consumer.outcome == 'success' && 'passed' || 'failed' }}"
            executed="${{ steps.consumer.outcome == 'success' && 'true' || 'false' }}"
            if [ "$consumer" = dotnet ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_NATIVE_DOTNET_TEST=1 node --test tests/native-dotnet.test.mjs && npm run test:type-corpus:dotnet && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=dotnet node --test tests/type-corpus-reviewed-native.test.mjs && LEAN_BRIDGE_DOTNET_CALLABLE_TEST=1 node --test tests/dotnet-callables.test.mjs tests/dotnet-callable-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_DOTNET_COMPOUND_TEST=1 node --test tests/dotnet-compounds.test.mjs tests/dotnet-compound-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_DOTNET_LIST_TEST=1 node --test tests/dotnet-lists.test.mjs tests/dotnet-list-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_DOTNET_ALIAS_TEST=1 node --test tests/dotnet-aliases.test.mjs"
              if [ "${{ steps.ordinary_dotnet.outcome }}" != success ] || [ "${{ steps.type_corpus_dotnet.outcome }}" != success ]; then
                test_result=failed
                executed=false
              fi
            fi
            if [ "$consumer" = jvm ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_NATIVE_JVM_TEST=1 node --test tests/native-jvm.test.mjs && npm run test:type-corpus:jvm && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=java,kotlin node --test tests/type-corpus-reviewed-native.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_JVM_CALLABLE_TEST=1 node --test tests/jvm-callables.test.mjs tests/jvm-callable-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_JVM_COMPOUND_TEST=1 node --test tests/jvm-compounds.test.mjs tests/jvm-compound-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_JVM_LIST_TEST=1 node --test tests/jvm-lists.test.mjs tests/jvm-list-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_JVM_ALIAS_TEST=1 node --test tests/jvm-aliases.test.mjs"
              if [ "${{ steps.ordinary_jvm.outcome }}" != success ] || [ "${{ steps.type_corpus_jvm.outcome }}" != success ]; then
                test_result=failed
                executed=false
              fi
            fi
            if [ "$consumer" = ruby ]; then
              consumer_command="$consumer_command && LEAN_BRIDGE_NATIVE_RUBY_TEST=1 node --test tests/native-ruby.test.mjs && npm run test:type-corpus:ruby && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=ruby node --test tests/type-corpus-reviewed-native.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUBY_CALLABLE_TEST=1 node --test tests/ruby-callables.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUBY_COMPOUND_TEST=1 node --test tests/ruby-compounds.test.mjs tests/ruby-compound-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUBY_LIST_TEST=1 node --test tests/ruby-lists.test.mjs tests/ruby-list-contract.test.mjs"
              consumer_command="$consumer_command && LEAN_BRIDGE_RUBY_ALIAS_TEST=1 node --test tests/ruby-aliases.test.mjs"
              if [ "${{ steps.ordinary_ruby.outcome }}" != success ] || [ "${{ steps.type_corpus_ruby.outcome }}" != success ]; then
                test_result=failed
                executed=false
              fi
            fi
            node scripts/consumer-ci.mjs record --consumer "$consumer" --output "build/consumer-ci/results/$consumer.json" --performance "build/consumer-ci/performance/$consumer.json" --test-result "$test_result" --package-installation "$executed" --real-lean-execution "$executed" --command "$consumer_command"
          done
      - name: Upload managed consumer observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-results-managed-${{ github.sha }}
          path: build/consumer-ci/results/*.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Enforce managed consumer support
        if: steps.consumer.outcome != 'success' || steps.ordinary_dotnet.outcome != 'success' || steps.type_corpus_dotnet.outcome != 'success' || steps.ordinary_jvm.outcome != 'success' || steps.type_corpus_jvm.outcome != 'success' || steps.ordinary_ruby.outcome != 'success' || steps.type_corpus_ruby.outcome != 'success'
        run: exit 1

  wasi-consumer:
    name: WIT and WASI Component Model
    runs-on: ubuntu-24.04
    timeout-minutes: 120
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Install dependencies without lifecycle scripts
        run: npm ci --ignore-scripts
      - name: Install the archive and invoke the component through Wasmtime
        id: consumer
        continue-on-error: true
        run: npm run test:consumer:wasi
      - name: Compile ordinary Lean APIs and consume relocated WIT packages
        id: ordinary_wit
        continue-on-error: true
        env:
          LEAN_BRIDGE_NATIVE_WIT_TEST: "1"
        run: |
          sudo apt-get update && sudo apt-get install -y build-essential pkg-config zstd
          bash scripts/bootstrap-toolchains.sh --lean-only
          bash scripts/bootstrap-toolchains.sh --wasm-tools-only
          nix build .#wasmtime-c-api --out-link build/wasmtime-c-api
          export LEAN_BRIDGE_WASMTIME_C_API="$PWD/build/wasmtime-c-api"
          source scripts/env.sh
          node --test tests/native-wit.test.mjs
          LEAN_BRIDGE_WIT_CALLABLE_COMPONENT_TEST=1 node --test tests/wit-callable-contract.test.mjs
          LEAN_BRIDGE_WIT_CALLABLE_TEST=1 node --test tests/wit-callables.test.mjs
          test -s build/callables/wit.json
          LEAN_BRIDGE_WIT_COMPOUND_TEST=1 node --test tests/wit-compounds.test.mjs tests/wit-compound-contract.test.mjs tests/wit-compound-conversions.test.mjs
          test -s build/compounds/wit.json
          test -s build/compounds/wit-conversions.json
          LEAN_BRIDGE_WIT_LIST_TEST=1 node --test tests/wit-lists.test.mjs tests/wit-list-contract.test.mjs tests/wit-list-conversions.test.mjs
          test -s build/lists/wit.json
          test -s build/lists/wit-conversions.json
          LEAN_BRIDGE_WIT_ALIAS_TEST=1 node --test --test-concurrency=1 tests/wit-aliases.test.mjs tests/wit-alias-contract.test.mjs tests/wit-alias-conversions.test.mjs
          test -s build/aliases/wit.json
          test -s build/aliases/wit-conversions.json
      - name: Compare installed WIT packages with the shared Lean corpus
        id: type_corpus_wit
        continue-on-error: true
        run: |
          export LEAN_BRIDGE_WASMTIME_C_API="$PWD/build/wasmtime-c-api"
          source scripts/env.sh
          npm run test:type-corpus:wit-wasi && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=wit-wasi node --test tests/type-corpus-reviewed-native.test.mjs
          LEAN_BRIDGE_CHAR_PROFILES=wit-wasi node --test tests/native-char.test.mjs
          LEAN_BRIDGE_WORD_PROFILES=wit-wasi node --test tests/native-words.test.mjs
          test -s build/type-corpus/wit-wasi.json
          test -s build/type-corpus/reviewed-native-wit-wasi.json
      - name: Record WIT and WASI observations
        if: always()
        shell: bash
        run: |
          test_result="${{ steps.consumer.outcome == 'success' && steps.ordinary_wit.outcome == 'success' && steps.type_corpus_wit.outcome == 'success' && 'passed' || 'failed' }}"
          executed="${{ steps.consumer.outcome == 'success' && steps.ordinary_wit.outcome == 'success' && steps.type_corpus_wit.outcome == 'success' && 'true' || 'false' }}"
          node scripts/consumer-ci.mjs record --consumer wit-wasi --output build/consumer-ci/results/wit-wasi.json --performance build/consumer-ci/performance/wit-wasi.json --test-result "$test_result" --package-installation "$executed" --real-lean-execution "$executed" --command "npm run test:consumer:wasi && LEAN_BRIDGE_NATIVE_WIT_TEST=1 node --test tests/native-wit.test.mjs && LEAN_BRIDGE_WIT_CALLABLE_COMPONENT_TEST=1 node --test tests/wit-callable-contract.test.mjs && LEAN_BRIDGE_WIT_CALLABLE_TEST=1 node --test tests/wit-callables.test.mjs && LEAN_BRIDGE_WIT_COMPOUND_TEST=1 node --test tests/wit-compounds.test.mjs tests/wit-compound-contract.test.mjs tests/wit-compound-conversions.test.mjs && LEAN_BRIDGE_WIT_LIST_TEST=1 node --test tests/wit-lists.test.mjs tests/wit-list-contract.test.mjs tests/wit-list-conversions.test.mjs && LEAN_BRIDGE_WIT_ALIAS_TEST=1 node --test --test-concurrency=1 tests/wit-aliases.test.mjs tests/wit-alias-contract.test.mjs tests/wit-alias-conversions.test.mjs && npm run test:type-corpus:wit-wasi && LEAN_BRIDGE_REVIEWED_NATIVE_PROFILES=wit-wasi node --test tests/type-corpus-reviewed-native.test.mjs"
      - name: Upload WIT shared corpus evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: type-corpus-wit-wasi-${{ github.sha }}
          path: |
            build/type-corpus/wit-wasi.json
            build/type-corpus/reviewed-native-wit-wasi.json
            build/char-native/wit-wasi.json
            build/word-native/wit-wasi.json
            build/callables/wit.json
            build/compounds/wit.json
            build/compounds/wit-conversions.json
            build/lists/wit.json
            build/lists/wit-conversions.json
            build/aliases/wit.json
            build/aliases/wit-conversions.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Upload WIT and WASI observations
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-results-wasi-${{ github.sha }}
          path: build/consumer-ci/results/*.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
      - name: Enforce WIT and WASI support
        if: steps.consumer.outcome != 'success' || steps.ordinary_wit.outcome != 'success' || steps.type_corpus_wit.outcome != 'success'
        run: exit 1

  docker-engine:
    name: Pinned Docker build path
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Restore pinned compiler toolchains
        uses: actions/cache@v5
        with:
          path: .toolchains
          key: consumer-docker-${{ runner.os }}-${{ hashFiles('scripts/bootstrap-toolchains.sh', 'containers/builder/manifest.json', 'lean-toolchain') }}
      - name: Install dependencies and pinned toolchains
        run: |
          npm ci --ignore-scripts
          npm run bootstrap
      - name: Build the pinned builder image
        run: npm run build:builder-image
      - name: Check the mounted repository ownership boundary
        run: npm run test:builder-ownership
      - name: Execute the clean consumer through Docker
        env:
          LEAN_BRIDGE_BUILD_BACKEND: docker
        run: npm run test:consumer:docker-ci

  perl-consumer:
    name: Perl
    uses: ./.github/workflows/perl-consumer.yml

  support-summary:
    name: Publish consumer support summary
    if: always()
    needs:
      - documentation
      - reviewed-ir-admission
      - nix-cache
      - node-consumers
      - browser-consumer
      - php-consumers
      - native-consumers
      - managed-consumers
      - perl-consumer
      - wasi-consumer
      - docker-engine
    runs-on: ubuntu-24.04
    steps:
      - name: Check out the support reporter
        uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Download consumer observations
        uses: actions/download-artifact@v5
        with:
          pattern: consumer-results-*-${{ github.sha }}
          path: build/consumer-ci/results
          merge-multiple: true
      - name: Validate all fourteen targets and publish the table
        run: node scripts/consumer-ci.mjs summary --results build/consumer-ci/results --output build/consumer-ci/report.json
      - name: Upload the combined support report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: consumer-support-report-${{ github.sha }}
          path: build/consumer-ci/report.json
          if-no-files-found: error
          overwrite: true
          retention-days: 30
