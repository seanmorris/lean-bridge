# Lean Alpha

Generated package documentation fixture for the canonical release manifest contract.

The consumer API uses direct functions and classes. Packaging backends must copy this document without changing its component or version identity.
