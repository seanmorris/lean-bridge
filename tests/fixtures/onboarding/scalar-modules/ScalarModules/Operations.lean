namespace ScalarModules.Operations
/-- Double a natural number. -/
def double (a : Nat) : Nat := a * 2
/-- Triple a natural number. -/
def triple (a : Nat) : Nat := a * 3
/-- Preserve a signed integer. -/
def signed (a : Int) : Int := a
end ScalarModules.Operations
