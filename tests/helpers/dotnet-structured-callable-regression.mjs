/**
 * Preserve every generated file for previously admitted C# copied fixtures.
 *
 * @file
 */
import assert from "node:assert/strict";
import { canonicalJson, sha256 } from "../../src/capsule/node.mjs";
import { generateCopiedDotnetPackage } from "../../src/backends/dotnet/copied-values.mjs";
import { callableReviewedIr } from "./callable-fixture.mjs";
import { dotnetCallableSignatures } from "./dotnet-callable-fixture.mjs";
import { collectionReviewedIr } from "./collection-fixture.mjs";
import { compoundReviewedIr } from "./compound-fixture.mjs";
import { listReviewedIr } from "./list-fixture.mjs";
import { nativeAliasReviewedIr } from "./native-alias-fixture.mjs";
import { nativeVariantReviewedIr } from "./native-variant-fixture.mjs";

export const dotnetStructuredRegressionFixtures = {
	callables: () => callableReviewedIr(dotnetCallableSignatures)
	, collections: collectionReviewedIr, compounds: compoundReviewedIr
	, lists: listReviewedIr, aliases: nativeAliasReviewedIr
	, variants: nativeVariantReviewedIr
};

/**
 * Compare all generated bytes with an independently rendered predecessor.
 *
 * @param record - Frozen source identities and generated-file observations.
 */
export const assertDotnetStructuredCodegenRegression = record => {
	assert.equal(record.schemaVersion, 1); assert.equal(record.planNode, 1219);
	assert.equal(record.kind, "dotnet-structured-codegen-regression");
	assert.match(record.baselineRevision, /^[a-f0-9]{40}$/);
	assert.deepEqual(record.fixtures.map(item => item.name), Object.keys(dotnetStructuredRegressionFixtures));
	for(const fixture of record.fixtures)
	{
		const ir = dotnetStructuredRegressionFixtures[fixture.name]();
		assert.equal(fixture.bindingIrSha256, sha256(canonicalJson(ir)));
		const files = generateCopiedDotnetPackage(ir);
		assert.deepEqual(fixture.files, Object.fromEntries(Object.entries(files).map(([path, source]) => [path, { bytes: Buffer.byteLength(source), sha256: sha256(source) }])));
		assert.equal(fixture.identicalToPredecessor, true);
	}
};
