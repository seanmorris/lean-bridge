{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:lean-bridge:schema:publish-manifest:v1",
  "title": "Lean Bridge authorized publication plan",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "predicateType",
    "mode",
    "createdAt",
    "authorization",
    "selection",
    "policy",
    "targets",
    "omitted"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "predicateType": { "const": "urn:lean-bridge:publication-plan:v1" },
    "mode": { "const": "authorized-no-publish" },
    "createdAt": { "type": "string", "format": "date-time" },
    "authorization": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "sha256",
        "candidatePath",
        "candidateId",
        "sourceRevision",
        "artifactInventorySha256",
        "publicationIndexPath",
        "publicationIndexSha256"
      ],
      "properties": {
        "path": { "$ref": "#/$defs/path" },
        "sha256": { "$ref": "#/$defs/sha256" },
        "candidatePath": { "$ref": "#/$defs/path" },
        "candidateId": { "$ref": "#/$defs/sha256" },
        "sourceRevision": { "type": "string", "minLength": 1 },
        "artifactInventorySha256": { "$ref": "#/$defs/sha256" },
        "publicationIndexPath": { "$ref": "#/$defs/path" },
        "publicationIndexSha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "selection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["allTargets", "requested", "plannedEcosystems"],
      "properties": {
        "allTargets": { "type": "boolean" },
        "requested": { "$ref": "#/$defs/strings" },
        "plannedEcosystems": { "$ref": "#/$defs/strings" }
      }
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "networkAccessPerformed",
        "externalRegistryWritesPerformed",
        "credentialsRead",
        "immutableAuthorizedArtifacts",
        "idempotentExecutionRequired"
      ],
      "properties": {
        "networkAccessPerformed": { "const": false },
        "externalRegistryWritesPerformed": { "const": false },
        "credentialsRead": { "const": false },
        "immutableAuthorizedArtifacts": { "const": true },
        "idempotentExecutionRequired": { "const": true }
      }
    },
    "targets": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/target" }
    },
    "omitted": {
      "type": "array",
      "items": { "$ref": "#/$defs/omitted" }
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "path": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$))(?!.*\\\\).+$"
    },
    "strings": {
      "type": "array",
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "file": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "path", "bytes", "sha256"],
      "properties": {
        "kind": { "type": "string", "minLength": 1 },
        "path": { "$ref": "#/$defs/path" },
        "bytes": { "type": "integer", "minimum": 0 },
        "sha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "backendPlan": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "bytes", "sha256"],
      "properties": {
        "path": { "$ref": "#/$defs/path" },
        "bytes": { "type": "integer", "minimum": 0 },
        "sha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "target": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "order",
        "candidateId",
        "ecosystem",
        "name",
        "version",
        "target",
        "operation",
        "destination",
        "coordinate",
        "backendPlan",
        "archives",
        "credentialEnvironment",
        "idempotencyKey"
      ],
      "properties": {
        "order": { "type": "integer", "minimum": 1 },
        "candidateId": { "$ref": "#/$defs/sha256" },
        "ecosystem": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "minLength": 1 },
        "target": { "type": "string", "minLength": 1 },
        "operation": { "enum": ["publish", "retain"] },
        "destination": {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "endpoint"],
          "properties": {
            "kind": { "type": "string", "minLength": 1 },
            "endpoint": { "type": ["string", "null"] }
          }
        },
        "coordinate": { "type": "string", "minLength": 1 },
        "backendPlan": { "$ref": "#/$defs/backendPlan" },
        "archives": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/file" }
        },
        "credentialEnvironment": { "$ref": "#/$defs/strings" },
        "idempotencyKey": { "$ref": "#/$defs/sha256" }
      }
    },
    "omitted": {
      "type": "object",
      "additionalProperties": false,
      "required": ["ecosystem", "name", "version", "target", "reason"],
      "properties": {
        "ecosystem": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 },
        "version": { "type": "string", "minLength": 1 },
        "target": { "type": "string", "minLength": 1 },
        "reason": { "type": "string", "minLength": 1 }
      }
    }
  }
}
