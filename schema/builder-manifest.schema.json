{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lean-wasm.invalid/schema/builder-manifest-v1.schema.json",
  "title": "Lean Bridge pinned builder manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion", "name", "platform", "sourceDateEpoch", "image", "bases",
    "files", "definitionSha256", "execution", "flakeOutputs"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "name": { "type": "string", "minLength": 1 },
    "platform": { "const": "linux/amd64" },
    "sourceDateEpoch": { "type": "integer", "minimum": 1 },
    "image": {
      "type": "object",
      "additionalProperties": false,
      "required": ["localTag", "configSha256", "publishedReference", "publicationStatus"],
      "properties": {
        "localTag": { "type": "string", "pattern": "^lean-bridge-builder:[0-9a-f]{16}$" },
        "configSha256": { "$ref": "#/$defs/sha256" },
        "publishedReference": {
          "oneOf": [
            { "type": "null" },
            { "type": "string", "pattern": "@sha256:[0-9a-f]{64}$" }
          ]
        },
        "publicationStatus": { "enum": ["local-reproducible", "published"] }
      }
    },
    "bases": {
      "type": "array",
      "minItems": 2,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["role", "reference", "version"],
        "properties": {
          "role": { "enum": ["nix-store", "runtime"] },
          "reference": { "type": "string", "pattern": "@sha256:[0-9a-f]{64}$" },
          "version": { "type": "string", "minLength": 1 }
        }
      }
    },
    "files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "sha256"],
        "properties": {
          "path": { "type": "string", "minLength": 1 },
          "sha256": { "$ref": "#/$defs/sha256" }
        }
      }
    },
    "definitionSha256": { "$ref": "#/$defs/sha256" },
    "execution": {
      "type": "object",
      "additionalProperties": false,
      "required": ["user", "uid", "gid", "locale", "timezone", "sourceMount", "outputMount", "network", "entrypoint"],
      "properties": {
        "user": { "type": "string", "minLength": 1 },
        "uid": { "type": "integer", "minimum": 0 },
        "gid": { "type": "integer", "minimum": 0 },
        "locale": { "type": "string", "minLength": 1 },
        "timezone": { "type": "string", "minLength": 1 },
        "sourceMount": { "type": "string", "minLength": 1 },
        "outputMount": { "type": "string", "minLength": 1 },
        "network": { "type": "string", "minLength": 1 },
        "entrypoint": { "type": "string", "minLength": 1 }
      }
    },
    "flakeOutputs": {
      "const": ["universal-release-bundle", "release-rehearsal"]
    }
  },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
  }
}
